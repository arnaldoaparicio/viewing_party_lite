<h1>User Login</h1>

<%= form_with url: 'login', local: true do |f| %>
    <%= f.label :email, 'Email', ai%>
    <%= f.text_field :email %>

    <%= f.label :password, 'Password' %>
    <%= f.password_field :password %>

    <%= f.submit 'Login' %>
<% end %>